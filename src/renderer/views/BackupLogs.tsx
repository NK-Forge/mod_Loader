// src/renderer/views/BackupLogs.tsx
// Parked placeholder for a possible future backup-log UI.
// Intentionally exposes no renderer IPC surface while the view is not mounted.

export default function BackupLogs() {
  return null;
}
